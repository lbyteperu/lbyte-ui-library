import React, { ReactNode } from "react";
export type MenuMobileProps = {
    children?: ReactNode;
};
declare const MenuMobile: ({ children }: MenuMobileProps) => React.JSX.Element;
export default MenuMobile;
